# test-sops.yaml is machine-local, gitignored
# test-age-key.txt is committed: it only decrypts dummy test data (not real secrets)
test/test-sops.yaml

# Never commit unencrypted secrets
secrets/secrets.yaml.plaintext
*.plaintext

# Nix build results
result
result-*

# VM disk images
*.qcow2
