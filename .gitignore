# Test secrets — age key and sops config are machine-local, gitignored
# test-secrets.yaml is sops-encrypted and safe to commit
test/test-age-key.txt
test/test-sops.yaml

# Never commit unencrypted secrets
secrets/secrets.yaml.plaintext
*.plaintext

# Nix build results
result
result-*
